version: '3.8'

services:
  target-app:
    image: python:3.9-slim
    working_dir: /app
    volumes:
      - ./app.py:/app/app.py
    command: sh -c "pip install flask && python app.py"
    ports:
      - "5000:5000"

  break-tester:
    build: .
    depends_on:
      - target-app